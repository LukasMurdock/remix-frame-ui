# Changesets

This directory stores pending release notes and version bumps for publishable packages.

- Add a changeset in any pull request that changes public behavior.
- The release workflow opens a version PR from these files.
- Merging that version PR publishes to npm.
