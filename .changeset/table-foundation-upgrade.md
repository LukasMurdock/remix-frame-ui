---
"@lukasmurdock/remix-ui-core": minor
"@lukasmurdock/remix-ui-components": minor
"@lukasmurdock/remix-ui-styles": minor
---

Add shared headless table primitives in core for sorting, selection, and async server data loading with cancellation and stale-response protection.

Upgrade DataTable with row interaction callbacks, selection ergonomics, server dataSource loading, and column/sort presentation hooks.

Publish a semver-stable DataTable styling contract with `data-part` hooks and `--rf-data-table-*` CSS variables.
